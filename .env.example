# Upstream OpenAI-compatible API to proxy to
UPSTREAM_BASE_URL=http://localhost:4000/v1

# API key for the upstream service (leave empty if none)
UPSTREAM_API_KEY=

# Default Langfuse host (used when X-Langfuse-Host header is not provided)
LANGFUSE_DEFAULT_HOST=https://cloud.langfuse.com
